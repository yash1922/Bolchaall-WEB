{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd .. && pnpm install --frozen-lockfile && pnpm --filter web build",
  "installCommand": "echo skip-vercel-install (handled in buildCommand)",
  "outputDirectory": ".next",
  "regions": ["bom1"]
}
